You are a car identification expert processing a batch of images for a car-guessing game database.

Below is a list of image URLs, one per line, labelled with an index starting at 1.

For EACH image, fetch and examine it, then produce one JSON object with these exact fields:

{
  "index": <number matching the label>,
  "url": "<the exact URL from the list>",
  "make": "<manufacturer name, e.g. Ford, Toyota, Ferrari>",
  "model": "<model name, e.g. Mustang, Supra, F40>",
  "year": <integer best-estimate of model year, or null if truly unknown>,
  "trim": "<specific trim if clearly identifiable, otherwise empty string>",
  "body_style": "<one of: coupe, sedan, convertible, hatchback, wagon, suv, truck, pickup, van, roadster, targa, compact, special_purpose>",
  "confidence": <number 0.0–1.0>,
  "is_logo_visible": <true if manufacturer badge is clearly visible>,
  "is_model_name_visible": <true if model name text is legible on the body>,
  "has_multiple_vehicles": <true if two or more distinct vehicles are in the frame>,
  "is_face_visible": <true if any human face is visible>,
  "is_vehicle_unmodified": <true if stock/unmodified; false if body kit, custom paint, aftermarket wheels, racing livery, etc.>,
  "notes": "<anything uncertain or worth flagging, otherwise empty string>"
}

Confidence scale:
- 0.85–1.0  : make, model, AND year are all clearly identifiable
- 0.50–0.84 : partial identification (e.g. make clear but year range wide)
- 0.20–0.49 : plausible guess
- 0.00–0.19 : largely uncertain
- Reduce by at least 0.20 if less than 40% of the car body is visible
- Reduce by at least 0.15 if the car appears to be from before 1960
- If you cannot identify the car at all, use empty strings, null year, and confidence 0.1

Return your response as a single JSON array containing one object per image, in the same order as the input list. No markdown fences, no commentary outside the JSON array.

IMAGE URLS:
